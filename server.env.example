# Copy to server.env and edit.
REMOTE_HOST=195.208.16.1
REMOTE_PORT=49588
REMOTE_USER=user
REMOTE_DIR='~/alpha_scoring'

# Prefer SSH keys. If password auth is unavoidable, set REMOTE_PASSWORD locally
# in server.env; that file is ignored by git.
# REMOTE_PASSWORD=
